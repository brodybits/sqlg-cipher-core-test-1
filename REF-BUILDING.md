# Building SQLiteGlue-core

## Normal build

*First* initialize with the `gluegentools` & `sqlite-amalgamation` subprojects:

$ `make init`

Then to build:

$ `make`

## Regenerage Java & C glue code

$ `make regen`

